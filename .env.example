# VibeMon Local intentionally has no cloud endpoints or authentication tokens.
# The collector listens only on http://127.0.0.1:19280.

# Optional: override the local registry cache directory for testing.
# VIBEMON_REGISTRY_CACHE_DIR=/tmp/vibemon-registry
